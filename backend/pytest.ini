[pytest]
testpaths = .
# Exclude the live-Postgres connectivity script (a manual tool, not a unit test) and
# deselect tests marked `network` so the default run is hermetic / CI-safe.
addopts = -m "not network" --ignore=test_db_connection.py
markers =
    network: tests that require live network/yfinance access (skipped by default)
